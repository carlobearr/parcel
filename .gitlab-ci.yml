stages:
  - build
  - test
  - deploy

build_job:
  stage: build
  tags:
    - node
  script:
    - npm install
    - cd frontend
    - npm install
    - npm run build
    - echo build_passed!

# test_job:
#   stage: test
#   tags:
#     - node
#   script:
#     - npm install
#     - npm run test
#     - echo test_passed!

deploy:
  stage: deploy
  script:
  - dpl --provider=heroku --app=pelican-express-development --api-key=$HEROKU_API_KEY